name: PR Labeler

on:
  pull_request:

jobs:
  label:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Label Pull Request
        uses: actions/labeler@v5
